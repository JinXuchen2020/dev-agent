# 阶段五：前沿特性与收尾

> 学习目标：锦上添花——代码沙箱闭环、性能压测达标、BDD 全量覆盖、简历作品集包装。

## 学习目标

- [ ] **Docker.DotNet**：.NET 操作 Docker 容器、镜像、网络的核心 API
- [ ] **代码沙箱安全**：容器隔离、资源限制、网络白名单、执行超时的实际配置
- [ ] **SerpAPI / Google Search**：SK 集成外部 API 实现多步调研链
- [ ] **性能调优**：从 30s 优化到 10s 的实战经验（缓存、并行、模型选择）
- [ ] **BDD 全量覆盖**：为历史代码补验收场景的策略和优先级
- [ ] **简历作品集包装**：如何将工程化项目转化为面试素材

## 前置依赖

- [ ] 阶段四已完成并提交
- [ ] 阶段四的性能基准已跑通（P95 < 30s）
- [ ] Docker Desktop 可正常操作容器（`docker ps` 无报错）

## 任务清单

- [ ] **Code Agent 闭环**：Docker.DotNet 操作沙箱，生成-运行-调试-修复闭环（**知识点**：Docker SDK + 安全配置） 🔍 强制：合入前必须走 `ddd-code-reviewer`，核对阶段五验收标准「生成-运行-调试-修复闭环」——重点验证闭环是否真能自修复（失败精准回打），而非只生成一次。
- [ ] **Research Agent**：SK 集成 SerpAPI，实现多步调研（**知识点**：外部 API 集成 + 链式调用） 🔍 强制：合入前必须走 `ddd-code-reviewer`，核对阶段五验收标准「多步搜索 → 结构化报告」——重点验证多步链真实串联、外部 API 真实调用（非伪造）。
- [ ] **蓝图对齐（来自 2026-07-16 设计评审）**：上述两闭环须对齐重写后的附录 C——① 失败修复走 **critic 循环**（F2：结构化 diff 精准回打，而非整轮重跑）；② 上下文走统一 **`WorkflowContext`** + 逐步摘要压缩（F3/C.3.1：防 token 爆炸，不堆全量历史）；③ 若引入多 Agent 协商须用 `negotiation` 预设的真实 selection/termination（C.5），禁止顺序发言退化。🔍 合入前 `ddd-code-reviewer` 报告须写明"已核对 C.2/C.3.1/C.5/C.6"。
- [ ] **性能压测与优化**：达到 P0 目标（并发 5 工作流、步骤 P95 < 10s、模型调用 P95 < 15s）
- [ ] 补全 BDD 全量验收用例，接入 CI/CD（**知识点**：测试分层 + CI 集成）
- [ ] 整理文档与简历作品集描述（**知识点**：技术影响力包装）

## 验收标准

1. Code Agent 能生成代码 → 运行 → 发现错误 → 修复 → 重新运行，形成闭环
2. Research Agent 能接收一个开放问题 → 多步搜索 → 输出结构化报告
3. 性能达到 P0 目标（8.6 全部 7 项指标）
4. 所有关键路径都有 BDD 验收场景覆盖
5. 简历中有一段 200 字的技术描述，突出 .NET 工程化 + AI 融合

▶ **设计评审关（动手前强制）**：进入本 Phase 前须已过 `blueprint-architecture-review`（见 phase-1 §0-1）。Code Agent 闭环 / Research Agent 等"叙事性能力"尤其要先确认蓝图范式无误，再进 §0 的 `ddd-code-reviewer` 强制审查。

## 0. Quality Skill Routing Policy（质量 Skill 路由策略）

本平台有两个互补 skill，职责不同、不可互相替代：

| 模块类型 | 强制 Skill | 目的 |
|----------|-----------|------|
| 实现"叙事性蓝图能力"的模块（编排器 / 状态机 / 协作引擎 / 沙箱闭环 / SSE 广播 / 监控指标 / RAG / Tool Calling / 模型路由等——**类名即承诺某种能力**） | **`ddd-code-reviewer`**（对抗式审查） | 验证实现行为是否忠于蓝图、依赖是否真实使用、注册接口方法是否非空壳 |
| 纯基础设施 / 结构卫生模块（仓储 / DI / EF 映射 / Redis / CRUD 控制器 / 配置 / CI） | `ddd-phase-quality-gate`（静态结构门禁） | DI / DDD 层 / EF / 并发 / 密封 / 守卫等结构卫生 |

**硬性规则（WHY）**：`ddd-phase-quality-gate` 的 "Blueprint Drift" 仅查"蓝图声明要做、但被标记未来的功能"，**不查"实现行为 vs 蓝图叙事"的深度一致性**。凡是"类名/接口名承诺了某种能力"的模块，都是"名不副实现"的高风险区，必须由 `ddd-code-reviewer` 把关。

**`ddd-code-reviewer` 报告必须包含**：对所审模块，显式写出"已核对的蓝图章节 / 验收标准"（例如 "verified against 附录 C.6 / §8.2 / 阶段 X 验收标准"）。缺此项即视为未通过。

### Phase 5 强制范围（高风险叙事性模块）

- **Code Agent 闭环**（Docker 沙箱生成-运行-调试-修复）：核对阶段五验收标准；重点验证闭环是否真能自修复（失败精准回打开发 Agent），而非只生成一次。
- **Research Agent**（SerpAPI 多步调研）：核对阶段五验收标准；重点验证多步链是否真实串联、外部 API 是否真实调用（非伪造结果）。
- 注：性能压测 / BDD 覆盖 / 简历包装属测试与文档工作，走 `ddd-phase-quality-gate` 或单独测试流程。

> 规划提示：Phase 5 尚未开始，本 §0 要求在此阶段启动前即明确——上述两个闭环模块合入前**必须**走 `ddd-code-reviewer`。

## 学习笔记

### 第一天（YYYY-MM-DD）

```

```

### 第二天（YYYY-MM-DD）

```

```

## 进度

- **开始日期**：
- **完成日期**：
- **完成度**：█░░░░░░░░░ 0%

## 回顾（完成后填写）

### 做得好的

### 下次改进

### 对蓝图文档的反馈
