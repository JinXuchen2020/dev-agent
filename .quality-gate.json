{
  "phase": "ci-e2e-2026-09-04-double-handler-fix",
  "reviewer": "ddd-code-reviewer PASSED (聚焦式修复：根因分析复核——MediatR 12.4.1 ServiceRegistrar 对 INotificationHandler 走 addIfAlreadyExists=true 分支（普通 AddTransient 非 TryAdd），与 Infrastructure 显式注册构成双重注册、每通知处理两次；无 P0/P1 open)",
  "structureGate": "N/A (聚焦式 CI 修复，非阶段收尾)",
  "codebaseOptimizer": "PASSED (Round 1, 0 open, scoped to this fix)",
  "cleared": true,
  "reportRef": "docs/quality/ci-e2e-2026-09-04-double-handler-gate.md",
  "notes": "CI E2E 复跑双败修复（2026-09-04）：1) F40 exactly-1 断言得 2——根因是 WorkflowStartedEventHandler 等领域事件处理器被双重注册（AddMediatR 程序集扫描 + Infrastructure 显式 AddScoped，MediatR 12 对 INotificationHandler 非 TryAdd），每次 run 产生 2 条 ExecutionLog，与「POST /workflows 留两条日志」历史观察吻合；修复=删除 Infrastructure 的 7 处显式注册（Infrastructure 程序集无 INotificationHandler 实现，grep 证实），扫描为唯一注册源。2) F35 combobox click 被 antd selection-item span 拦截 60s（该步骤首次在 CI 跑到，前次卡在确认按钮）——照 credentials.steps.ts:28 先例改 click({force:true})×2。校验：build 0/0；后端全量回归结果见提交信息；bddgen exit 0、tsc 0 error。 E2E 超时基线协调（2026-09-04，与本次提交一并入库）：playwright 测试级 timeout 30s→60s，F29 真实 LLM 场景改用 Gherkin 标签 @timeout:180000 单独放大，agent.steps 的等待预算由 testInfo.timeout 推导（预留 30s 给前置步骤、可用 E2E_AGENTIC_ANSWER_TIMEOUT 覆盖但不超预算），消除「步骤等 90s、测试 30s 被强杀」的错位；本地 bddgen exit 0 / tsc 0 error。"
}
