{
  "phase": "f18-dashboard-charts",
  "reviewer": "ddd-code-reviewer PASSED (P0/P1/P2/P3 = 0 open；穷尽分析无缺陷：静默崩溃路径均兜住——ExecutionLogRepository.GetByTenantAsync 已 Include(Entries) 使延迟聚合有效、Conversation.TotalTokenUsage 构造即 =new TokenUsage(0,0) 非 null、ExecutionLogEntry.Duration 为 TimeSpan 值类型、from>to 由 Controller 兜底 400；控制流 Controller→IMediator.Send→Handler 取四仓储+ITenantProvider 全程 ct 透传、租户过滤到位、无 N+1（单次拉取后内存日桶聚合）；测试覆盖 handler 6 例 + 集成 2 例（含倒序范围 400）)",
  "structureGate": "ddd-phase-quality-gate PASS (P0/P1/P2/P3 = 0 open；12 类全扫：DI 注册缺口/DDD 层违规/EF 映射缺口/缺 CancellationToken/缺 internal sealed/并发风险/缺 null 守卫/API 基础设施/蓝图漂移/缺 XML 注释/Swagger/死代码 均 PASS；硬编码值 P3×2（Take(8)/366 天上限魔法数）→ 提取为命名常量 TopWorkflowsLimit/MaxRangeDays 修复并重建 0/0 + 全方案 270/270 绿；新公开成员（含两仓储范围重载）均中文 XML 注释。清单嵌入 features/dashboard-charts.md §F18 Quality Gate Checklist)",
  "codebaseOptimizer": "codebase-optimizer PASSED (0 open；七维聚焦健康检查 F18 改动范围：无桩代码（Handler 真实聚合/Controller 真实派发/仓储真实查询）、无 XSS（DashboardPage 仅 antd+recharts 声明式组件，无 dangerouslySetInnerHTML）、前端类型显式无 any（tsc 0 error）、无未捕获 Promise（useApiState 统一处理）、hook 依赖 [range] 正确、无未用导入（build 0 警告）、无硬编码密钥。后端 dotnet build 0/0、dotnet test 270/270；前端 tsc 0 + vitest 38/38)",
  "cleared": true,
  "reportRef": "docs/quality/f18-dashboard-charts-gate.md",
  "notes": "F18 · Dashboard 图表充实（运行分析看板）：把仅 4 计数卡的 Dashboard 升级为运行分析看板。(1) 后端新增 GET /api/v1/analytics/summary [Authorize]（AnalyticsController 仅注入 IMediator），单一 DashboardSummaryDto 一次返回全部图表数据（KPIs + ExecutionsByDay/TokenByDay/ConversationsByDay/LatencyByDay/TopWorkflows），租户隔离经 ITenantProvider，应用层按日桶聚合（v1，留 SQL GROUP BY 下沉余地），含 from>to→400、范围>366 天→400 边界。(2) Application 层 GetDashboardSummaryQuery/Handler，拉取区间内租户原始行（仓储 IExecutionLogRepository/IConversationRepository 新增日期范围重载，Include(Entries) 保证延迟聚合）后内存聚合。(3) 前端引入 recharts@^2.15.4（设计 D4 文档备选，React 19 兼容更稳、包体更轻），DashboardPage 重写：Segmented 7/14/30 天 + 6 KPI 卡 + 6 图（执行趋势堆叠柱/成功率折线/Token 面积/会话量柱/平均延迟折线/Top 工作流横向柱），空态复用 Empty、错误复用 ErrorState；api.ts 加 getDashboardSummary、types 对齐 DashboardSummary 系列（camelCase 与后端一致）；locales zh-CN/en-US 新增 pages.dashboard 图表键严格镜像（对称性测试通过）。(4) 测试：GetDashboardSummaryQueryHandlerTests 6 例 + EndpointContractTests 集成 2 例（200 形状 + 倒序 400）；全方案 270/270 绿。设计偏离：图表库选用 recharts（设计默认 @ant-design/plots 的备选），6 图+6 KPI 与设计的图表集合完全一致；D2/D3 默认采用标签 t() 化 + 已认证即可读。质量报告 docs/quality/f18-dashboard-charts-gate.md。"
}
