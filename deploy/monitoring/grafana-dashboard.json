{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "title": "API Request Rate",
      "type": "timeseries",
      "targets": [{ "expr": "rate(api_requests_total[1m])", "legendFormat": "requests/sec" }]
    },
    {
      "title": "API Error Rate",
      "type": "timeseries",
      "targets": [{ "expr": "rate(api_errors_total[1m])", "legendFormat": "errors/sec" }]
    },
    {
      "title": "Request Duration P95",
      "type": "timeseries",
      "targets": [{ "expr": "histogram_quantile(0.95, rate(api_request_duration_ms_bucket[5m]))", "legendFormat": "p95 (ms)" }]
    },
    {
      "title": "Model Call Total",
      "type": "timeseries",
      "targets": [{ "expr": "rate(model_call_total[1m])", "legendFormat": "{{provider}}/{{model}}" }]
    },
    {
      "title": "Model Call Duration P95",
      "type": "timeseries",
      "targets": [{ "expr": "histogram_quantile(0.95, rate(model_call_duration_ms_bucket[5m]))", "legendFormat": "{{provider}}/{{model}}" }]
    },
    {
      "title": "Workflow Step Duration P95",
      "type": "timeseries",
      "targets": [{ "expr": "histogram_quantile(0.95, rate(workflow_step_duration_ms_bucket[5m]))", "legendFormat": "{{step_name}}" }]
    },
    {
      "title": "Workflow Completion Counter",
      "type": "timeseries",
      "targets": [{ "expr": "rate(workflow_completed_total[1m])", "legendFormat": "{{result}}" }]
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["agent-platform"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timepicker": {},
  "timezone": "",
  "title": "Agent Platform Dashboard",
  "uid": "agent-platform",
  "version": 1
}
